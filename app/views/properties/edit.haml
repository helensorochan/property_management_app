.sheet_title
  - content_for :page_title, t('.title')

- if PropertyPolicy.new(current_user, @property).destroy?
  = link_to t('.delete'),
            developer_building_complex_property_path(@developer, @building_complex, @property),
            data: { confirm: t('.confirm_delete') },
            method: :delete

= render 'form', url: developer_building_complex_property_path(@developer, @building_complex, @property)